# Продолжение веб-замера GEO — лист «Август 2026»

> Набор для досъёма веб-замера живой выдачи по листу **«Август 2026»**
> (таблица SEO_GEO позиции LUIS+, spreadsheet `16GXxvAtU4JnG9OxYRpTXkYxSKgwGbp1chMH87rHx8YA`, sheetId `2087466600`).
> Создан 06.09.2026. Причина паузы: лимит **200 WebSearch на сессию** (общий на сессию и всех субагентов, включая remote; сбрасывается новой сессией).

## Состояние на 06.09.2026 (после 4-й сессии)
- Замерено и записано в лист: **720 / 1309** запросов (`geo_results.json` — {запрос: [домены топ-10]}); luis.ru в топ-10 у **27** (в 4-й сессии +6: «СКУД Бастион» №3, «СКУД ELSYS» №2, «блок речевого оповещения рупор 300» №4, «оповещатель охранно пожарный световой табло» №3, «оповещатель световой табло про» №2, «радиоканальное световое табло» №2).
- Осталось: **580** запросов — список в `geo_remaining.json` (порядок = порядок листа, уникальные без дублей). Это ~3 сессии по 7 батчей.
- В 4-й сессии снято 175 запросов (7 батчей × 25, волны 5+2 агентов, файлы wave5_a..e + wave6_a..b) — лимит 200 WebSearch выбран почти полностью (~180).
- Пройденные темы 4-й сессии: СКУД (системы, замки электромагнитные, считыватели, бренды Sigur/PERCo/Парсек/Бастион), турникеты и шлагбаумы, СОУЭ и речевое оповещение (Рупор/Соната/Рокот), антитеррор, световые табло «Выход», начало ИБП. Паттерн прежний: в товарной рознице доминируют tinko/aktivsb/satro-paladin/vseinstrumenti/ozon; luis.ru всплывает на брендовых и узкопродуктовых запросах.
- Следующая сессия начинается с ИБП/бесперебойного питания (см. первые позиции `geo_remaining.json`).
- ГигаЧат (колонка N) — прогон завершён ранее полностью (1309/1309, LUIS+ = 0).

## Как продолжить (в новой сессии)
1. Проверить остаток: `geo_remaining.json` может быть уже частично отработан — перед стартом пересчитать: остаток = все запросы из `august_row_map.json`, которых нет в `geo_results.json` (нормализация: lower + схлопнуть пробелы).
2. Резать остаток на батчи по 25 → волны по 5 агентов-поисковиков (промпт: WebSearch по запросу дословно; при нерелевантности один доп. поиск с «купить»/«поставщик»; вернуть строго JSON {запрос: [первые 10 уникальных доменов без www]}; пустой список = «не искал», НЕ «нет в выдаче»).
3. После каждой волны: сохранить JSON агентов в файлы и запустить
   `python3 process_wave.py <файлы...>` — он сам:
   - пропустит пустые списки и уже замеренные запросы,
   - извлечёт позицию luis.ru → колонки **F, H, J** (одинаково — источник один),
   - извлечёт позиции 14 конкурентов → колонки **AT..BG**,
   - допишет `geo_results.json`.
   Пути в скриптах — от папки скрипта; auth — сервисный аккаунт `keys/pme-sheets-0591ecbcdf46.json` (JWT+openssl, `write_august.py`).
4. Бюджет поисков: при лимите 200 → ~8 батчей на сессию (~200 запросов). Если поднят `CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION` до 2000 — весь остаток за одну сессию (волнами по 5 агентов, ~9 волн).

## Что ещё не заполнено в листе (ждёт данных от Дарьи)
- **SEO: D (luis.ru) и P..AC (14 конкурентов)** — нужна свежая cookie PR-CY (Copy as cURL со страницы конкурентов) → снять API PR-CY, правило `-1`/пусто → `100+`.
- **Алиса: L (LUIS+) и AE..AR (конкуренты)** — нужен API-ключ YandexGPT (сервисный аккаунт `geo-luis`, каталог `b1gqp4uoft2jaiu80qv0`, область `yc.ai.foundationModels.execute`); раннеры по образцу `giga_run.py` / памяти проекта (alisa_run.py, alisa_comp_run.py). Ключ после прогона отозвать.
- **ГигаЧат: N** — если прогон 06.09 не дописал, продолжить `giga_run.py` (ключ `keys/gigachat_key.txt`, resume из `giga_progress.json`), затем записать N только для ok=True.

## Раскладка листа «Август 2026» (= «Июль 2026»)
A=Запрос · B=Вордстат · D=SEO Яндекс LUIS+ · F/H/J/L/N = GEO LUIS+ (ChatGPT/Gemini/Perplexity/Алиса/ГигаЧат) · P..AC = SEO конкуренты · AE..AR = GEO Алиса конкуренты · AT..BG = GEO ChatGPT конкуренты. Разделители C,E,G,I,K,M,O,AD,AS. Шапка 4 строки, данные с 5-й. Порядок конкурентов: etm.ru, rsvet.ru, rubezh.ru, dssl.ru, bolid.ru, tinko.ru, layta.ru, satro-paladin.com, rutektd.ru, iss.ru, dean.ru, mossb.ru, garantgroup.com, argus-spectr.ru.

## SEO — снято 06.09 вечером (частично: 5 конкурентов из 14)
- Источник: **PR-CY API** `a.pr-cy.ru/api/v3.1.0/keywords/extended?filter[projectId]=155277&filter[searchOptionsIds]=127670&filter[dateFrom]=…&filter[dateTo]=…` + заголовок `X-Csr: 1` + Cookie из `keys/prcy_cookie.txt` (прислана 06.09, обновлять ежемесячно). Скрипт: `prcy_seo.py` (fetch / write, справка внутри).
- В проекте PR-CY 1293 ключа ≈ наш лист (сопоставление: lower+ё→е, второй проход без дефисов/точек, `мм→mm`). Записано **1285 строк**: D (luis.ru) + P,Q,R,S,T (etm, rsvet, rubezh, dssl, bolid; id 57049–57053 по порядку интерфейса). Даты замера 30.08→03.09, берём последнюю. Правило `-1`/пусто → `100+`.
- **Осталось 9 конкурентов (колонки U..AC): tinko, layta, satro-paladin, rutektd, iss / dean, mossb, garantgroup, argus-spectr.** Процедура: Дарья в PR-CY «Изменить конкурентов» → ставит следующую пятёрку → дождаться пересчёта позиций (обычно следующий апдейт проекта) → `prcy_seo.py fetch` → определить НОВЫЕ projectCompetitorId по опорным ключам → `prcy_seo.py write tinko.ru=<id> …`. Потом третий заход с последней четвёркой.
- Строки без SEO: 13 длинных GEO-вопросов среза (их нет в PR-CY — норма) + «стрелец»/«кабель канал dkc» и ещё ~10 ключей PR-CY, которых нет в листе (не переносил).

## ⚠️ Важно для финализации GEO
- В хвосте листа (строки ~1296–1322) — блок «Срез 20.08»: заголовок (строка 1296, значения в неё НЕ писать) + 13 длинных вопросов + 13 коротких запросов-ДУБЛЕЙ основного массива.
- `process_wave.py` пишет по map query→row (последнее вхождение) — первые строки дублей остаются пустыми. **После завершения всего веб-замера прогнать финальный row-based проход**: для КАЖДОЙ строки row map взять её запрос → geo_results → записать F/H/J + AT..BG (по образцу финальной дозаписи ГигаЧата write_giga.py, которая делалась по номерам строк).
